package wi.annotator;

public class Formatter {
	
	
	
	//ΚµΜε(pos0-pos1)
//	public static Entity formatEntity(String annotation){
//		String str = annotation.substring(annotation.indexOf("(")+1,annotation.indexOf(")"));
//		String entity = annotation.substring(0,annotation.indexOf("("));
//		int p0 = Integer.valueOf(str.substring(0,str.indexOf("-")));
//		int p1 = Integer.valueOf(str.substring(str.indexOf("-")+1));
//		return new Entity(entity,p0,p1);
//	}

}
